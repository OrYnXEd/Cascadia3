public abstract class FoxScoringCard extends ScoringCard {
    public FoxScoringCard(String id) {
        super(id, "Fox");
    }

    public int calculateScore(Board board) {
        int score = 0;
        // Add scoring logic here
        return score;
    }
}
