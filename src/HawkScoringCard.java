public class HawkScoringCard extends ScoringCard {
    public HawkScoringCard(String id, String cardType) {
        super(id, cardType);
    }

    @Override
    public int calculateScore(Board board) {
        // Add code here to calculate the score based on the game rules for the Hawk Scoring Card.
        return 0;
    }
}
